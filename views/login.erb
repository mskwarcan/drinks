<% if !@today %>
<h2>Login</h2>
<form class="infield" id="login" action="/login" method="post">
<p>
	<label for="email">Email</label>
	<input type="text" name="email" id="email" />
</p>
<p>
	<label for="password">Password</label>
	<input type="password" name="password" id="password" />
</p>
<p>
	<input type="submit" class="submit" value="Login" />
</p>
</form>

<p><b class="pink">Not Registered?</b> Sign up for free <a href="/register">here</a>.</p>

<h3>For Bar Owners/Managers</h3>
	
<p>Simply create and account and begin entering information about your bar. You can then begin entering detailed information like your daily drink specials, if you have cover on certain nights, if you have live music on certain nights, a description of your bar, and more. It's completely free and you have nothing to lose! By not registering, this means your bar will not appear on the search results. It is possible to have multiple bars.</p>

<% else %>
<h3><%= @today %> Specials</h3>
<div class="searchBar">
	<% if !@beer.empty? %>
		<p><b>Beer</b></p>
		<% @beer.each do |special| %>
				<% bar = Bar.first(:id => special.bar_id)%>
				<div class="special">
					<span class="drink_price">$<%= special.price %></span> <%= special.drink_name %><br />
					&#64;<a href="/show/<%= special.bar_id %>"><%= bar.name %></a><br />
				</div>
		<% end %>
	<% end %>
	<% if !@pitcher.empty? %>
		<p><b>Pitchers</b></p>
		<% @pitcher.each do |special| %>
				<% bar = Bar.first(:id => special.bar_id)%>
				<div class="special">
					<span class="drink_price">$<%= special.price %></span> <%= special.drink_name %><br />
					&#64;<a href="/show/<%= special.bar_id %>"><%= bar.name %></a><br />
				</div>
		<% end %>
	<% end %>
	<% if !@mixed.empty? %>
		<p><b>Mixed Drinks</b></p>
		<% @mixed.each do |special| %>
				<% bar = Bar.first(:id => special.bar_id)%>
				<div class="special">
					<span class="drink_price">$<%= special.price %></span> <%= special.drink_name %><br />
					&#64;<a href="/show/<%= special.bar_id %>"><%= bar.name %></a><br />
				</div>
		<% end %>
	<% end %>
	<% if !@shot.empty? %>
		<p><b>Shots</b></p>
		<% @shot.each do |special| %>
				<% bar = Bar.first(:id => special.bar_id)%>
				<div class="special">
					<span class="drink_price">$<%= special.price %></span> <%= special.drink_name %><br />
					&#64;<a href="/show/<%= special.bar_id %>"><%= bar.name %></a><br />
				</div>
		<% end %>
	<% end %>
	<% if !@wine.empty? %>
		<p><b>Wine</b></p>
		<% @wine.each do |special| %>
				<% bar = Bar.first(:id => special.bar_id)%>
				<div class="special">
					<span class="drink_price">$<%= special.price %></span> <%= special.drink_name %><br />
					&#64;<a href="/show/<%= special.bar_id %>"><%= bar.name %></a><br />
				</div>
		<% end %>
	<% end %>
</div>
<% end %>