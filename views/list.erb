<h1><%= @title %></h1>

<% @bars.each_with_index do |bar,i| %>
<p>
	<a href="/click/<%= bar.id %>"><%=bar.name %></a><br />
	<b>Address:</b><br />
	<%= bar.address %><br />
	<%= bar.city %>, <%= bar.state %> <%= bar.zip %><br />
	<a href="/delete/<%= bar.id %>">Delete</a><br />
	<a href="/edit/<%= bar.id %>">Edit</a><br />
</p>
<% end %>
</p>
<% t = Time.now 
   t = t - + (60 * 60 * 5)%>
Today is <%= t.strftime("%A") %>:
The drink specials for today are:<br />
<% @specials.each_with_index do |special,i| %>
    <% if special.day_of_week == Time.new.strftime("%A") %>
		<%=special.drink_name %> $<%=special.price %> <br />
	<% end %>
<% end %>
